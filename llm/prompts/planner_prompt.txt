Generate ONLY JSON.

Schema:

{
  "title": string,
  "bpm": integer,
  "key": string,
  "sections": [
    {
      "name": string,
      "bars": integer,
      "midi_prompts": [string],
      "transition": string
    }
  ]
}

Requirements:
- DURATION: MUST be between 4:00 and 6:30 minutes (OBLIGATORY). 
- STRUCTURE: STRICTLY follow the section sequence and transitions described in the expansion prompt.
- DIVERSITY: Each section MUST have a unique identity (varying density, energy, and instrumentation).
- 10-12 TRACKS: Every midi_prompt MUST specify at least 10 tracks for a professional "wall-of-sound" arrangement.
- DROPS: Every song MUST have at least one 'drop' transition (sudden silence/velocity dip) EVERY 32 BARS. 
- SECTION CONTRAST: Every section MUST have a unique identity and different instrument combinations.
- PIANO SOLO: Every song MUST include a dedicated 'Piano Solo' section (at least 8-16 bars).
- HARMONY: Follow the specific scale/harmony mentioned in the production plan.
- PIANO: Every song MUST have 2 to 3 DISTINCT Piano tracks with style-based patterns.
- NO MUTE TRACKS: Every track MUST have notes in the MIDI, but they can be silenced by the 'transition: drop' field.
- PRODUCTION SECRETS: Describe tracks using professional terms: 'Sidechained Pumping', 'Octave Jumps', 'Open Voicings'.
- FX: Incorporate the specific effects mentioned (e.g., Bitcrusher, Gated Reverb, Sidechain).
- No explanations, no markdown, valid JSON only.
