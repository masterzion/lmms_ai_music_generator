Generate ONLY JSON.

Schema:

{
  "title": string,
  "bpm": integer,
  "key": string,
  "sections": [
    {
      "name": string,
      "bars": integer,
      "transition": string,
      "tracks": [
        {
          "name": string,
          "midi_prompt": string,
          "polyphony": integer,
          "grid": string,
          "density": float,
          "pitch_range": [integer, integer],
          "is_drum": boolean
        }
      ]
    }
  ]
}

Requirements:
- MIDI PROMPT: The midi_prompt field MUST NOT be empty. Use descriptive musical instructions like "Haunting minor melody with staccato notes" or "Uplifting lead arpeggio in 1/16th notes".
- PITCH RANGE: Use MIDI note numbers (0-127) for pitch_range. Example: [36, 48].
- DURATION: MUST be between 4:00 and 6:30 minutes (OBLIGATORY). 
- STRUCTURE: STRICTLY follow the section sequence and transitions described in the expansion prompt.
- DIVERSITY: Each section MUST have a unique identity (varying density, energy, and instrumentation).
- NO COMMENTS: Do NOT include ANY comments (no /* */ or //) inside the JSON.
- NO PLACEHOLDERS: Every section MUST include the FULL list of tracks. Never use "same as previous" or similar placeholders.
- 10-12 TRACKS: Every section MUST have at least 10-12 tracks for a professional "wall-of-sound".
- DROPS: Every song MUST have at least one 'drop' transition EVERY 32 BARS.
- PIANO SOLO: Every song MUST include a dedicated 'Piano Solo' section (8-16 bars).
- PIANO: Every song MUST have 2 to 3 DISTINCT Piano tracks.
- PRODUCTION SECRETS: Use 'Sidechained Pumping', 'Octave Jumps', 'Open Voicings'.
- No explanations, no markdown, valid JSON only.
